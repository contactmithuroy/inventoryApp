import action from './actions'
import getters from './getters'
import mutations from './mutations'

const state = {
    categories :[],
}

export default{
    state,
    action,
    getters,
    mutations,
}